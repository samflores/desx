[package]
name = "desx"
version = "0.1.0"
edition = "2024"

[dependencies]
freedesktop-desktop-entry = { version = "0.8.1", default-features = false }
freedesktop-icons = "0.4.0"

[dev-dependencies]
tempfile = "3.10"

[profile.release]
strip = "symbols"
